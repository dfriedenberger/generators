version: "3.9"
services:
  {{#services}}
    peer-{{name}}:
      build: {{build}}
      image: {{image}}
{{#port}}
      ports:
        - {{port}}:{{port}}
      environment:
        - SERVER_PORT={{port}}
{{/port}}
  {{/services}}